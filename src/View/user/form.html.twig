<div class="container ">
{% block subtitle %}{% endblock %}
    <form class="" method="post">
    {% if error.errorEmail is not empty %}
        {{ error.errorEmail}}
        {% endif %}
        <div class="form-group">
            <input class="form-control" type="email" name="email" placeholder="Entrez votre email">
        </div>
        <div class="form-group">
            <input class="form-control" type="password" name="password"
                placeholder="Entrez votre mot de passe">
        </div>
        <div class="d-flex flex-column justify-content-center"> 
            <button type="submit"><a href="/connexion"></a>Connexion</button>

            <p class="mt-3">Pas encore de compte ?</p>
            <a href="/inscription" type="submit" class="btn btn-primary">Inscription </a>
        </div>
    </form>
</div>